<%= form_for [commentable, @comment] do |f| %>
  <div class="form-group">
    <%= f.text_area :comment_body, class: "form-group", placeholder: "Create a Comment" %>
  </div>

  <%= f.submit "Create Comment", class: "btn btn-primary" %>
<% end %>